[build-system]
requires = [
    "colorama",
    "defusedxml",
    "lief",
    "msgpack>=1.0.0",
    "pycryptodomex",
    "setuptools",
    "toml",
    "wheel",
]
build-backend = "setuptools.build_meta"