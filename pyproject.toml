[build-system]
requires = [
    "colorama",
    "defusedxml",
    "lief>=0.16.0",
    "msgpack>=1.0.0",
    "pycryptodomex",
    "setuptools",
    "toml",
    "wheel",
]
build-backend = "setuptools.build_meta"