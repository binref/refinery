[run]
omit =
    refinery/lib/thirdparty/*
    refinery/units/obfuscation/*
    refinery/lib/annotations.py

[report]
exclude_also =
    if TYPE_CHECKING:
    except ImportError
    except KeyboardInterrupt
    except ArgparseError
    except Exception
    raise NotImplementedError
    yappi